def handle(payload: dict) -> dict:
    return {
        "status": "stubbed",
        "skill": "explain_ip_risk",
        "input": payload,
        "message": "Future narrative risk explanation for conflict, stale evidence, or source inconsistency.",
    }


def handle(payload: dict) -> dict:
    return {
        "status": "stubbed",
        "skill": "check_ip_availability",
        "input": payload,
        "message": "Future deterministic + AI-assisted IP availability explanation placeholder.",
    }

